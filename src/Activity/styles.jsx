import { styled } from '@mui/material/styles';

const useStyles = () => ({
  container: {
    padding: '16px', // equivalent to theme.spacing(3) in MUI v4
  },
  chart: {
    marginTop: '32px', // equivalent to theme.spacing(4) in MUI v4
  },
  history: {
    marginTop: '16px', // equivalent to theme.spacing(2) in MUI v4
  },
});

export default useStyles;
